/*import React from 'react';
import Radium from 'radium';
//import Collapse, { Panel } from 'rc-collapse';
import Accordion from '../../components/Accordion';
import Panel from '../../components/AccordionPanel';
import PanelHeader from '../../components/AccordionHeader';
import PanelContentOne from '../PanelContentOne';
import PanelContentTwo from '../PanelContentTwo';
import autoBind from 'react-autobind';


class CampaignsAccordion extends React.Component {
    constructor(props) {
	super(props);
	autoBind(this);
	this.state= {
	    activeKey: "4",
	};
    }
    onChange(activeKey) {
	this.setState({
	    activeKey,
	});
    }
    render() {
	const activeKey = this.state.activeKey;
	return (
		<div>
		<Accordion />
		</div>
		)
    }
    
}

export default Radium(CampaignsAccordion)
*/
